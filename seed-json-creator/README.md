Create json files for 4 player lunar with all unlocks, for the four random starting areas, using rnssp. Running the code outputs the csv's in the working dir.
```rs
cargo run --release
```

When/if an update is needed, make sure to upgrade rnssp in `Cargo.toml`
